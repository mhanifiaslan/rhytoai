# Opsiyonel ağır bağımlılıklar — yalnızca lokal geliştirmede.
# Tamamı yüz analizi (v1 kapsamı dışı) içindir; Cloud Run imajına girmez.
# mediapipe lokal ortamla eşit sürüme sabitlendi (Tasks API / ctypes arayüzü
# sürüme duyarlı).
mediapipe==0.10.35
opencv-python
deepface
tf-keras
